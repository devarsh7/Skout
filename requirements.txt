# =============================================================================
# Skout – Influencer Discovery & Filtering Platform
# Python 3.10+ recommended
# =============================================================================

# ---------- Web framework ----------
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2

# ---------- Frontend ----------
streamlit==1.38.0
streamlit-extras==0.4.7
streamlit-tags==1.2.8
plotly==5.24.1
pandas==2.2.2

# ---------- Database (relational) ----------
SQLAlchemy==2.0.35
alembic==1.13.3
# psycopg2-binary==2.9.9     # uncomment for Postgres in prod
# asyncpg==0.29.0

# ---------- Vector DB ----------
pinecone==5.0.1
# chromadb==0.5.5            # free local alt, kept commented

# ---------- Embeddings ----------
sentence-transformers==3.1.1     # FREE local embeddings (all-MiniLM-L6-v2)
# openai==1.51.0                  # paid embeddings (also handles LLM below)

# ---------- Anthropic (Creator Agent) ----------
anthropic>=0.40.0

# ---------- LLM providers ----------
# Ollama used via REST, so langchain-ollama handles it:
langchain==0.3.1
langchain-core==0.3.6
langchain-community==0.3.1
langchain-ollama==0.2.0
langgraph==0.2.28
# langchain-anthropic==0.2.1     # paid: Claude (uncomment for prod)
# langchain-openai==0.2.1        # paid: OpenAI GPT-4o (uncomment for prod)

# ---------- MCP (Model Context Protocol) ----------
mcp>=1.0.0

# ---------- Auth & Security ----------
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
email-validator==2.2.0

# ---------- HTTP client ----------
httpx==0.27.2
requests==2.32.3

# ---------- Utilities ----------
python-dotenv==1.0.1
tenacity==8.5.0
loguru==0.7.2
rich==13.8.1
apscheduler==3.10.4

# ---------- Dev / Test ----------
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.6.8
